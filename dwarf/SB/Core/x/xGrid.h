/*
    Compile unit: C:\SB\Core\x\xGrid.h
    Producer: MW MIPS C Compiler
    Language: C++
    Code range: 0x00200690 -> 0x0020107C
*/
// Range: 0x200690 -> 0x20107C
void xGridCheckBound(class xGrid & grid /* r29+0x14C */, class xBound & bound /* r2 */, class xQCData & qcd /* r29+0x148 */) {
    // Blocks
    /* anonymous block */ {
        // Range: 0x200690 -> 0x20107C
        class cb_ray_hits_ent cb; // r29+0x210
        class xBox box; // r29+0x1F0
        float ex; // r2
        float ez; // r1
        class xGridBound * cell; // r19
        unsigned short gx; // r29+0x130
        unsigned short gz; // r16
        class xGridBound * cell; // r19
    }
}

/*
    Compile unit: C:\SB\Core\x\xGrid.h
    Producer: MW MIPS C Compiler
    Language: C++
    Code range: 0x002AC450 -> 0x002ACF58
*/
// Range: 0x2AC450 -> 0x2ACF58
void xGridCheckBound(class xGrid & grid /* r23 */, class xBound & bound /* r2 */, class xQCData & qcd /* r29+0xF8 */, class cb_damage_ent cb /* r29+0xFC */) {
    // Blocks
    /* anonymous block */ {
        // Range: 0x2AC450 -> 0x2ACF58
        class xBox box; // r29+0x1C0
        float ex; // r2
        float ez; // r1
        class xGridBound * cell; // r16
        unsigned short gx; // r30
        unsigned short gz; // r16
        class xGridBound * cell; // r19
    }
}

/*
    Compile unit: C:\SB\Core\x\xGrid.h
    Producer: MW MIPS C Compiler
    Language: C++
    Code range: 0x002ACF60 -> 0x002AD7DC
*/
// Range: 0x2ACF60 -> 0x2AD7DC
void xGridCheckBound(class xGrid & grid /* r22 */, class xBound & bound /* r2 */, class xQCData & qcd /* r29+0xE8 */) {
    // Blocks
    /* anonymous block */ {
        // Range: 0x2ACF60 -> 0x2AD7DC
        class xBox box; // r29+0xF0
        float ex; // r2
        float ez; // r1
        class xGridBound * cell; // r16
        unsigned short gx; // r23
        unsigned short gz; // r16
        class xGridBound * cell; // r19
    }
}

/*
    Compile unit: C:\SB\Core\x\xGrid.h
    Producer: MW MIPS C Compiler
    Language: C++
    Code range: 0x002FB130 -> 0x002FBA0C
*/
// Range: 0x2FB130 -> 0x2FBA0C
void xGridCheckBound(class xGrid & grid /* r30 */, class xBound & bound /* r2 */, class xQCData & qcd /* r29+0x10C */) {
    // Blocks
    /* anonymous block */ {
        // Range: 0x2FB130 -> 0x2FBA0C
        class cb_cache_collisions cb; // r29+0x1D0
        class xBox box; // r29+0x1B0
        float ex; // r2
        float ez; // r1
        class xGridBound * cell; // r16
        unsigned short gx; // r29+0xF0
        unsigned short gz; // r21
        class xGridBound * cell; // r19
    }
}

