#include "zGust.h"

#include <types.h>











































#if 0
// WIP.
void zGustUpdateFX(float32 seconds)
{
    for (int32 i = 0; i < ngusts; i++)
    {
        zGust* curr = gusts[i];
        if (curr->flags & 1)
        {
            UpdateGustFX(curr, seconds);
        }
    }
}

#endif
